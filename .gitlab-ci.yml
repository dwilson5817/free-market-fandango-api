stages:
  - build

build-zip:
  image: python:3.12
  stage: build
  before_script:
    - apt update
    - apt install zip -y
  script:
    - pip install -r requirements.txt -t .
    - zip -r $CI_PROJECT_NAME.zip .
  artifacts:
    paths:
      - $CI_PROJECT_NAME.zip
